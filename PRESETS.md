# Presets

The built-in catalog exposes 80 named presets, including vocal, tilawah, podcast, speech, studio, music, ambient, cinematic, lo-fi, live, radio and creative families. Built-ins are generated from validated rack parameter snapshots. User presets serialize the full parameter map as versioned JSON in DataStore Preferences.
